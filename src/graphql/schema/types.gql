enum Role{
    USER
    ADMIN
    SUPER_USER
}

type User{
    _id: ID
    name: String
    role: [Role]
}

type Discount{
    description: String
    amount: Int
}

type ShowUsersConnection{
    pageInfo: Pagination
    total: Int
    edges: [User]
}